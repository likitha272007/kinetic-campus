Flask==3.0.3
mysql-connector-python==8.4.0
Werkzeug==3.0.3

# ── AI / GPU Features ──────────────────────────────────
# Install these for GPU-powered semantic search:

# 1. PyTorch — auto-uses NVIDIA GPU via CUDA if available
#    For NVIDIA GPU (CUDA 12.x):
#      pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#    For CPU only:
#      pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

torch
torchvision

# 2. Sentence Transformers — semantic similarity model (80MB download on first run)
sentence-transformers==3.0.1

# 3. Image processing
Pillow>=10.0.0
numpy>=1.24.0
